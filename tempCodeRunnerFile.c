f("\nPlease enter start city: ");
                // scanf("%[^\n]", &start);
                // printf("\nPlease enter destination city: ");
                // scanf("%[^\n]", &goal);
                // // startCityName = strdup(start);
                // // goalCityName = strdup(goal);
                // printf("startCityName: %s   ", start);
                // printf("goalCity